import { PasswordComplexity } from './password-complexity';

describe('PasswordComplexity', () => {
  it('should create an instance', () => {
    expect(new PasswordComplexity()).toBeTruthy();
  });
});
